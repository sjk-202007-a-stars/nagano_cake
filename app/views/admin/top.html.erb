<div class="container">
	<% flash.each do |key, value| %>
		<%= content_tag(:div, value, class:key) %>
	<% end %>
	<h3 class="item-headline">管理者画面</h3>
	<p class="daily-order">本日の注文件数
		<sapn class="mrg"><%= link_to admin_orders_path(order_sort: 2) do %><%= @order.count %><% end %> 件</sapn>
	</p>
</div>