<div class="container">
  <h4>商品詳細</h4>

  <div class="col-xs-3">
    <div class="items-image">
      <%= attachment_image_tag @item, :image, :fill, 300, 200, fallback: "no_image.jpg", size: '300x200' %>
    </div>
  </div>
  <div class="col-xs-4">
    <p>商品名　　<span><%= @item.name %></span></p>
    <p>商品説明　　<span><%= @item.introduction %></span></p>
    <p>ジャンル　　<span><%= @item.genre.name %></span></p>
    <p>税込価格（税抜価格）　　<span><%= @item.price_with_tax %>（<%= @item.price %>）円</span></p>
    <p>販売ステータス　　<span><%= @item.is_on_sale? %></span></p>
  </div>
</div>

<%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-primary" %>